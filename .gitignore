node_modules
package-lock.json
dist

# WordPress plugins (managed by wp-cli, except mu-plugins)
wp-content/plugins/
wp-content/upgrade/
wp-content/uploads/
wp-content/cache/

# WordPress configuration (never commit - contains credentials!)
wp-config.php

# Environment variables (never commit these!)
.env.development
.env.development.local
.env.production
.env.production.local
.env